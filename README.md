BasketRecommenderApi
====================

Java - NetBeans

Note; the current code is hardcoded to look for the file 'order_data.dat' in the [Tomcat] folder.  This file should be in the format of [orderId,productId].
